---
name: chinese-celestial-palace
description: "Use when creating, adapting, diagnosing, refining, or directly generating cinematic 中式天宫 imagery: credible Chinese timber palaces, cloud seas as spatial terrain, monumental scale, tiny human anchors, restrained vermilion–blue-gray–gold palettes, and physically plausible mythic light. Trigger for 中式天宫、东方仙宫、云海宫阙、天庭仙境、国风神话史诗、云上宫殿、仙侠电影概念图, coherent image series, or images that look generic, kitschy, structurally incoherent, or insufficiently Chinese, including built-in image generation and explicitly requested GPTX-compatible third-party API generation."
---

# Chinese Celestial Palace｜中式天宫

## 目标

把任意主题编译成可直接用于图像生成的专家级“中式天宫”提示词。以空间设计、镜头叙事和文化可信度建立风格，不依赖龙凤、灯笼、巨月、祥云等符号堆砌。

牢记核心公式：

> 建筑负责秩序，云海负责留白，光线负责神性，自然负责诗意，人物负责尺度与叙事。

## 加载参考资料

- 每次任务先读 [visual-dna.md](references/visual-dna.md)，锁定身份级视觉基因与文化边界。
- 新创作、改构图、制作组图时读 [composition-recipes.md](references/composition-recipes.md)，按叙事选择镜头原型。
- 每次编译提示词时读 [prompt-compiler.md](references/prompt-compiler.md)，使用槽位、母模板和模型适配器。
- 交付前必须读 [quality-gates.md](references/quality-gates.md)，完成硬失败检查与定向修正。

## 识别任务模式

选择一个或组合多个模式：

1. **创作**：从主题、故事、用途或一句话概念生成全新提示词。
2. **改写**：保留原提示词的主体和硬约束，将环境、构图、光色与渲染转译为中式天宫语法。
3. **诊断**：分析已有图像或提示词为什么“不够天宫、不够中式、太俗、太乱或太像游戏素材”，再给出修订版。
4. **系列**：设计 2–9 张同源但不重复的封面、分镜、壁纸或社媒组图。
5. **直接生图**：用户明确要求“画、生成、出图”时，先在内部完成编译与质检，再调用可用的图像生成能力；不要只交付提示词。

## 解析简报

提取以下信息；只有主体必需，其余可自动决策：

- 主体、动作、故事瞬间与情绪
- 使用场景：概念图、封面、海报背景、壁纸、分镜、产品视觉等
- 场景规模：单一地标或由多个城区、路径与基础设施组成的天界聚落
- 画幅、留字区、精确文字与安全区
- 机位高度、观察距离、俯仰倾角与等效焦段；不把“远距离”等同于“高机位”
- 人物数量、身份、服装、动作与是否必须无人
- 历史考据程度、指定朝代与允许的幻想程度
- 时间、天气、光线、色彩与材质
- 必须出现、必须保留、明确禁止的元素
- 目标模型或生成工作流

仅在以下缺口会显著改变结果时提一个简短问题：严格朝代考据、必须逐字生成的文案、主体是否保持现代形态、或互相冲突的硬约束。其他情况直接采用合理默认并用一句话说明假设。

默认采用：1.85:1 电影宽幅、可信但不宣称考据复原的神话化中国木构、一名极小人物、黎明或清晨、云海参与空间、一个主奇观、高明度云白与浅青灰中性基底、克制的朱砂与矿物青绿、局部暖琥珀受光、写实电影数字绘景。默认平台、长阶、桥、廊、露台和云港采用开放边界，不设栏杆、护栏或围栏；只在用户明确要求或严格历史考据构成硬约束时例外。不得覆盖用户已给出的相反要求。

先确定场景规模：

- **单体地标**：围绕一座主殿、天门、长廊、神木、孤峰或器物建立唯一空间中心。
- **天界聚落**：以一个主城区或神圣轴线统领 2–4 个次级城区，通过长阶、开放廊桥、庭院、云中航道、水系或浮岛通路显示可读的聚落连接；密度不得代替层次、留白和主次。

## 编译流程

按以下顺序完成设计；只呈现最终设计决定，不输出冗长内部推理：

1. **守住语义核心**：明确用户真正要讲的人、物、事件或观点。风格迁移不得把主体抹掉。
2. **判定场景规模**：单体地标只设一个主导目标；天界聚落先建立主城区、次级城区与可见通路，避免退化为一座孤殿或无关建筑散落在雾中。
3. **选择空间隐喻**：把“抵达、等待、发现、审判、离去、对谈”等叙事动词映射为一个构图原型。
4. **确定宏观几何**：只选择一套主秩序，如中轴、圆框、L 形横跨、C/S 形曲廊或偏置孤岛。
5. **拆分镜头决策**：分别说明机位高度、观察距离、俯仰倾角和等效焦段。想要巨物压迫时优先人眼高度或低机位；只在用户要求城区分布、浮岛关系或鸟瞰信息时使用高机位总览。
6. **设定天宫机制**：让宫阙通过云海托举、悬崖承托、天阶连接、云瀑或高空庭院存在；解释建筑如何落地，避免无逻辑漂浮。
7. **控制奇观预算**：只设一个主神迹，可增加一个低调辅助意象。巨月、神树、瀑布、光柱、圆环、神兽不可争抢焦点。
8. **建立深度阶梯**：优先构建 5–6 层；至少保留暗前框、人物落脚平面、中景建筑节奏、云雾遮挡带和远端目标中的四层。
9. **证明巨大尺度**：在完整环境画面中至少安排四类可见证据，优先从台阶级差、铺装模数、门洞、柱径、开间、桥宽、人物、重复构件向远处收敛、建筑延伸出画外中选择。人物必须有明确落脚面和接触关系。
10. **让云参与物理空间**：安排云的至少两种作用，如托举、遮显、穿廊、承接瀑布、捕捉逆光或分隔远近。
11. **统一建筑语汇与开放边界**：用柱网、开间、斗拱、深出檐、宽缓屋面、台基与廊桥建立中国性。默认不设栏杆、护栏或围栏，用铺装变化、标高差、柱列、水系、光线或构件节奏定义边界与通行。用户明确要求或严格历史考据构成硬约束时才允许栏杆。指定朝代时统一屋顶、构件、彩画与服饰语言。
12. **建立明亮而克制的色彩**：以高明度云白、珍珠灰、浅石色或浅青灰为中性基底，加入克制的朱砂、矿物青绿、黛青或暖金局部色彩，保持材质之间的色彩分离。不使用全局灰雾、脏灰滤镜、霓虹色或满屏红金。
13. **建立单一主光**：确定主光来源、冷暖关系、云顶与云底差异、体积散射和远景空气透视。
14. **按视觉权重写提示词**：先整体场景与构图，再空间关系、建筑、主体、云与光、材质、摄影和渲染。少用空泛质量词，多写可执行的空间关系。
15. **动态生成排除项**：只禁止与本次方案冲突的内容；默认加入“无栏杆、无护栏、无围栏、无全局灰雾、无脏灰滤镜”。用户明确要求或严格历史考据构成硬约束时，移除无栏杆排除项；用户明确要求的龙、霓虹、文字或群像也不得被模板误删。
16. **通过质量门槛**：总分至少 85/100，且无硬失败。失败时针对结构、空间或焦点重编译，不要继续堆关键词。

## 保持原创与克制

- 复用视觉语法，不机械复刻任何参考画面的具体宫殿、月门、人物位置或色彩分布。
- 默认让建筑与自然至少有一处不对称破缺，避免机械镜像。
- 除非用户要求严格复现，同系列相邻画面至少改变构图原型、相机位置、主奇观、路径形态、时间或人物策略中的三项。
- 不把“巨月＋圆门＋白衣背影”设为默认答案。
- 不用“8K、masterpiece、best quality”代替构图、材质和光线设计。
- 不声称参考风格属于严格的某一历史朝代；默认称为“神话化中国古典木构”。

## 输出契约

默认用中文，按以下顺序交付：

1. **创意定调**：一句话说明画面叙事与视觉抓手。
2. **构图蓝图**：列出场景规模、构图原型、机位高度、观察距离、俯仰倾角、等效焦段、前中远层次、主焦点、至少四类尺度证据、主奇观、主光和配色。
3. **可复制主提示词**：输出一段完整、无占位符、可直接粘贴的成品提示词。
4. **负面提示词／必须避免**：根据目标模型选择独立负面字段或自然语言约束。
5. **生成控制**：给出画幅、留字区、人物数量、主光和必要的模型适配建议；不要捏造不确定的专属参数。
6. **变化旋钮**：提供 2–4 个不会破坏视觉 DNA 的可选变化方向。
7. **质检结果**：只报告总分、假设和已解决的风险；不要展示内部推理过程。

若用户说“只要提示词”，仅输出主提示词和必要的负面提示词。若用户要求结构化交付，使用 [prompt-compiler.md](references/prompt-compiler.md) 中的 JSON 契约。

## 组图规则

- 先建立共同世界规则：建筑语汇、基础色盘、云层物理、材质和人物设定保持一致。
- 交替使用“轴向—偏置—轴向—长焦嵌套”或“建立—门槛—进入—行旅—神谕—终章”的镜头节奏。
- 每张只承担一个叙事动作和一个主奇观。
- 避免每张都用中央大殿、正面小人和同一时间光线。
- 为封面或海报预留文字时，用低细节云海、天空或暗墙形成连续安全区；不得把文字压在斗拱、人物或高反差云边上。

## 直接生成或编辑

### 选择生成通道

- 用户要求直接生图但未指定后端时，使用宿主提供的内置图像生成工具。
- 只有用户明确要求 GPTX、第三方 API 或 OpenAI Images 兼容接口时，才运行 [generate_image.py](scripts/generate_image.py)。第三方调用会把提示词发送到外部服务并可能产生费用，不得因内置工具失败而静默切换。
- 用户提供参考图做编辑时，先检查图片；明确保留主体、身份、构图或文字中的哪些部分，只迁移被要求的风格层。第三方路径未覆盖参考图、蒙版或编辑接口，继续使用内置工具。
- 任一通道失败时，报告真实错误和所用通道。不要自动换模型、自动重试或把 `gpt-image-2` 发送到 `/v1/chat/completions`。

### 使用 GPTX 兼容 API

1. 用户没有 Key 时，引导其访问 [GPTX.CC](https://www.gptx.cc/) 注册并进入控制台创建 API Key，确认该 Key 可调用 `gpt-image-2`。官网当前标注新用户注册即送 5 美元体验额度，具体活动与可用模型以控制台实时显示为准。
2. 指导用户在本机设置 `GPTX_API_KEY`，不得要求用户把完整 Key 贴进对话，也不得写入 `SKILL.md`、`README.md`、`AGENTS.md`、版本库、项目记忆或日志。macOS 的 zsh 可在运行 Agent 或脚本的同一终端中静默输入：

   ```bash
   read -s "GPTX_API_KEY?粘贴 GPTX API Key：" && export GPTX_API_KEY && echo
   ```

   设置完成后，用户只需回复「已设置，请用 GPTX 生图」。
3. 把必要排除项合并进自然语言主提示词，因为该路径只发送 `prompt`，不假设独立 `negative_prompt` 字段可用。
4. 从本 Skill 根目录运行：

   ```bash
   python3 scripts/generate_image.py \
     --prompt-file /absolute/path/prompt.txt \
     --size 2048x1152 \
     --output /absolute/path/result.png
   ```

5. 默认使用 `https://api.gptx.cc/v1/images/generations` 和 `gpt-image-2`。仅在用户明确提供兼容服务时，通过 `GPTX_BASE_URL` 和 `GPTX_IMAGE_MODEL` 覆盖；脚本固定 `n=1`。
6. 优先使用已验证像素尺寸：`1:1` 用 `2048x2048`，`3:4` 用 `1536x2048`，`4:3` 用 `2048x1536`，`9:16` 用 `1152x2048`，`16:9` 用 `2048x1152`。其他电影画幅保留在提示词中，并选择最接近的可用尺寸，不宣称精确比例。
7. 生成后打开实际图片，依据 [quality-gates.md](references/quality-gates.md) 检查。只有文件非空、可读取且视觉质检通过，才报告图片已生成。

生成结果出现泛东亚建筑、塑料 CG、云层贴图、人物漂浮或多奇观竞争时，依据 [quality-gates.md](references/quality-gates.md) 定向重做。
